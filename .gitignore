# Build outputs
bin/
obj/

# IDE
.vs/
.idea/
*.user
*.suo

# Local build config (contains absolute game path — do not commit)
Directory.Build.props

# Game interop DLLs (copyrighted — cannot be redistributed)
libs/game/

# Inspector tool output (development only)
Inspector/bin/
Inspector/obj/

# Release artifacts (generated locally by Publish-Release.ps1 — uploaded to GitHub, not committed)
*.zip
release-staging/

# Installer publish output
Installer/publish/

# Jekyll auto-generated config (created by CI from CNAME)
docs/_config_auto.yml
